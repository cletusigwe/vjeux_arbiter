import type { Config } from "tailwindcss";

const config: Config = {
  darkMode: ["class"],
  content: [
    "./src/pages/**/*.{js,ts,jsx,tsx,mdx}",
    "./src/components/**/*.{js,ts,jsx,tsx,mdx}",
    "./src/app/**/*.{js,ts,jsx,tsx,mdx}",
  ],
  theme: {
    fontFamily: {
      sans: ["ui-sans-serif"],
      serif: ["ui-serif"],
      mono: ["ui-monospace"],
    },
    extend: {
      fontFamily: {
        ibmMono: "IBM Plex Mono",
      },
      colors: {
        grey: "#181818",
        orange: "#cb4b16",
        markdown_yellow: "#b58900",
        markdown_blue: "#268bd2",
        markdown_yellowLight: "#FDE68A",
        markdown_red: "#dc322f",
        markdown_dark: "#002b36",
        markdown_ash: "#a1b4b8",
        markdown_markdown_blue: "#268bd2",
        markdown_emphasizedContent: "#586e75",
        markdown_emphasizedContentLight: "#93a1a1",
        markdown_backgroundHighlightDark: "#073642",
        markdown_bodyText: "#465c64",
        markdown_violet: "#6c71c4",
        markdown_magenta: "#d33682",
        markdown_cyan: "#2aa198",
        markdown_green: "#859900",
        background: "hsl(var(--background))",
        foreground: "hsl(var(--foreground))",
        card: {
          DEFAULT: "hsl(var(--card))",
          foreground: "hsl(var(--card-foreground))",
        },
        popover: {
          DEFAULT: "hsl(var(--popover))",
          foreground: "hsl(var(--popover-foreground))",
        },
        primary: {
          DEFAULT: "hsl(var(--primary))",
          foreground: "hsl(var(--primary-foreground))",
        },
        secondary: {
          DEFAULT: "hsl(var(--secondary))",
          foreground: "hsl(var(--secondary-foreground))",
        },
        muted: {
          DEFAULT: "hsl(var(--muted))",
          foreground: "hsl(var(--muted-foreground))",
        },
        accent: {
          DEFAULT: "hsl(var(--accent))",
          foreground: "hsl(var(--accent-foreground))",
        },
        destructive: {
          DEFAULT: "hsl(var(--destructive))",
          foreground: "hsl(var(--destructive-foreground))",
        },
        border: "hsl(var(--border))",
        input: "hsl(var(--input))",
        ring: "hsl(var(--ring))",
        chart: {
          "1": "hsl(var(--chart-1))",
          "2": "hsl(var(--chart-2))",
          "3": "hsl(var(--chart-3))",
          "4": "hsl(var(--chart-4))",
          "5": "hsl(var(--chart-5))",
        },
      },
      borderRadius: {
        lg: "var(--radius)",
        md: "calc(var(--radius) - 2px)",
        sm: "calc(var(--radius) - 4px)",
      },
    },
  },
  plugins: [require("tailwindcss-animate")],
};
export default config;
